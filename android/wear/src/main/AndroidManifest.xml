<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.type.watch" />

    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault">

        <uses-library android:name="com.google.android.wearable" android:required="true" />
        <meta-data android:name="com.google.android.wearable.standalone" android:value="false" />

        <!-- Heart-rate / step mirror while the phone records (opened by PlanActions). -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name" />

        <!-- Launch screen: Samsung-style sport picker. -->
        <activity
            android:name=".PlansActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SportMenuActivity"
            android:exported="false"
            android:label="@string/plans" />

        <activity
            android:name=".QuickGoalActivity"
            android:exported="false"
            android:label="@string/quick_goal" />

        <activity
            android:name=".PlanDetailActivity"
            android:exported="false"
            android:label="@string/plans" />

        <activity
            android:name=".StepEditActivity"
            android:exported="false"
            android:label="@string/plans" />

        <activity
            android:name=".RecordActivity"
            android:theme="@style/Theme.Aera.Record"
            android:exported="false"
            android:label="@string/app_name" />

        <!-- Foreground service that samples HR and streams it to the phone. -->
        <service
            android:name=".HrService"
            android:exported="false"
            android:foregroundServiceType="health" />

        <!-- Standalone recording (Phase 4): Health Services ExerciseClient, used when
             no phone is connected. -->
        <service
            android:name=".ExerciseService"
            android:exported="false"
            android:foregroundServiceType="health|location" />

        <!-- Receives step / cue / stop messages, and plan DataItem changes, from
             the phone. -->
        <service
            android:name=".PhoneListener"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <data android:scheme="wear" android:host="*" android:pathPrefix="/aera" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <data android:scheme="wear" android:host="*" android:pathPrefix="/aera/plan" />
            </intent-filter>
        </service>
    </application>
</manifest>
